<view class="container">
  <view wx:if="{{loading}}" class="loading-tip">加载中...</view>
  <empty-view wx:elif="{{list.length === 0}}" text="暂无被动升级配置" />
  <view wx:else>
    <view class="card" wx:for="{{list}}" wx:key="id">
      <view class="item-header">
        <text class="item-name">{{item.deviceName}}</text>
        <text class="action-del" catchtap="onDelete" data-id="{{item.id}}">删除</text>
      </view>
      <text class="info-text">版本: {{item.version}}</text>
    </view>
  </view>
  <view class="fab" bindtap="onAdd"><text class="fab-text">+</text></view>
  <view class="modal-mask" wx:if="{{showModal}}" bindtap="onCloseModal">
    <view class="modal-content" catchtap="noop">
      <text class="modal-title">添加被动升级</text>
      <view class="input-item">
        <text class="input-label">设备名称</text>
        <input class="input-field" placeholder="设备名称" data-field="deviceName" bindinput="onFormInput" value="{{form.deviceName}}" />
      </view>
      <view class="input-item" style="border-bottom:none">
        <text class="input-label">目标版本</text>
        <input class="input-field" placeholder="固件版本" data-field="version" bindinput="onFormInput" value="{{form.version}}" />
      </view>
      <view class="modal-footer">
        <button class="btn btn-default" bindtap="onCloseModal">取消</button>
        <button class="btn btn-primary" bindtap="onSubmit">确认</button>
      </view>
    </view>
  </view>
</view>
